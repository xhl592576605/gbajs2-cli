/**
 * Sample Test File
 * 
 * This is a sample test file to verify the Jest setup.
 */

describe('Sample Test Suite', () => {
  test('should pass a simple test', () => {
    expect(1 + 1).toBe(2);
  });
});